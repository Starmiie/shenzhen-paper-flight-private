# 验证记录

验证日期：2026-09-13。上游固定版本 d61d243bffcc2f24a34f0036cfb844d3abc81992。

## 构建与数据

- `npm run build`：TypeScript 严格检查与 Vite 生产构建通过。Babylon 主包体积警告保留，约 6.5 MB / gzip 1.45 MB。
- `npm test`：5 项通过。校验全部交付景观文件哈希/长度与 LFS 实体、13 个地点真实经纬度与游戏坐标一致性、源码依赖图不含被删除的系统、纯几何边界情况、所有植被/立面清单引用文件存在。
- Git LFS 的 229 个景观大文件独立对照上游 SHA-256 核验通过。交付资源清单包含 3,418 个城市文件，约 276 MB；并非所有文件会在首帧同时加载。
- 原地图中的 10 个地点与 3 个增量地点合并为 13 个。全部坐标与约定转换的偏差小于 0.01 游戏单位（原清单舍入），未凭空添加地理坐标。
- 150 个原近景立面瓦片均在交付目录；运行时保留原项目的按距离加载机制。

## 实际浏览器检查

使用 Codex 内置浏览器，开发入口 `http://127.0.0.1:5173/paper.html` 与生产静态入口 `http://127.0.0.1:4173/`。

已实际操作和观察：

1. 完成加载并从欢迎页进入原版三维深圳场景。南山后海、春笋、周边建筑与城市道路/地形可见；未加载人物或车辆模型。
2. 飞机持续前进，拖动后航向变化，空格进入悬停；可在建筑前取景。未逐栋验证整个场景的碰撞。
3. 清晨、午后、黄昏、深夜切换；夜间楼窗和公共灯光可见。
4. “感兴趣” → “想去”状态互换；同状态再次点击可取消，正式版计数实际从 0 → 1 → 0。
5. 旅程侧栏展示已标记地点、地图与真实 WGS84 坐标；核对人才公园真实地图链接使用 22.513660, 113.944170，经纬度和游戏 X/Z 分开。
6. 拍照模式调整镜头距离与视野；界面隐藏/恢复；实际生成 JPEG，照片缩略图带地点与游戏时间，并出现在本地照片记录中。
7. 拍照状态下，在多次后续操作与观察期间时钟保持 17:23；退出后恢复流动。
8. 开发入口刷新并重新加载后，“想去”标记与照片记录均恢复。
9. 生产入口能够独立加载，修复包装时遗漏的 `landscape/palm.glb` 后复测成功，新增资源清单引用检查防止再遗漏。修复前错误仍留在该浏览器的历史控制台日志；修复后当前包未新增同类加载错误。

正式版短时诊断样本：后海附近约 1,877 个场景网格，近景立面驻留 7 / 总共 150 瓦片，`failedTiles=[]`；清晨约 19.9 FPS、深夜约 25.1 FPS。它们只是当前机器、当时窗口和视角的瞬时样本，不是全程平均值，不代表所有设备。

截图：`screenshots/sunset-flight.png`，从实际生产页面截取，未用设计图替代。

## 明确的验证边界

- 未逐一绕飞全部建筑或验证每条道路；低处、窄缝和地形边缘的碰撞/相机裁剪是基础实现，仍可能出现穿插。
- 触控按钮与响应式布局已实现，桌面窄视口可显示；未在真实手机上进行长时间测试。
- 未进行跨浏览器、弱网、显卡丢失上下文或长时间稳定性测试。
- 完整城市资产较大，可能首载卡顿；四时段环境切换会有材质重编译，可能短暂停顿。
- 通过浏览器接口的自动点击偶有窗口尺寸变化/状态变化导致定位超时，已依据最新画面重新观察，未把超时点击视为成功。
- 独立 Playwright 启动本机 Chrome 受到环境限制，没有作为验证结果；以上实际交互来自正常内置浏览器。
- 部分测试照片/标记可能留在测试端口的浏览器存储中。它们不写入交付源码，也不会随文件带到另一浏览器。
- 本地许可审阅不等于发行法律审查，未部署公网。
